#include <bits/stdc++.h>
using namespace std;
int main(){
    int size = 0;
    string s="Emon";
    while(s[size]!='\0'){
        size++;
    }
    cout<<size<<endl;

    // '\0' is null character which is always there at the end of the string arr
    
}